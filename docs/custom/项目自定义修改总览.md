# 项目自定义修改总览

## 1. 项目身份

- Upstream：`marktext/marktext`
- Fork：`Ember-Dawn/marktext`
- 当前主要分支：`main`
- 当前基线版本：**MarkText v0.19.1**
- 主要目标平台：Windows 11 x64
- 核心原则：**稳定与性能优先，定制功能尽量以最小侵入方式实现**

当前 `main` 不是以 upstream 最新 `develop` 为基线，而是主动选定 v0.19.1 作为稳定开发基线。

## 2. 为什么选择 v0.19.1

曾经基于后续 `develop / 0.20.0-dev` 进行定制，但实际使用中出现 Markdown 打开和多标签切换明显变慢的问题。

重新测试纯净 v0.19.1 后，`pnpm run dev` 下的编辑、打开文件和标签切换速度恢复正常，因此决定：

> 后续个人定制优先建立在 v0.19.1 上，并把性能作为重要验收指标。

这意味着后续实现任何 UI 功能时，都应避免无必要的持续 watcher、重复计算、隐藏但仍挂载的大型组件，以及影响标签切换路径的重逻辑。

## 3. 已实现的自定义功能

### 3.1 右侧 TOC

状态：**已实现**

目标是在不破坏原有左侧 Files / Search / TOC 的情况下，增加独立右侧目录。

已实现能力：

- 保留左侧原生 Sidebar 与原生 TOC
- 新增独立右侧 TOC
- 左右 TOC 可同时显示
- Windows 自定义标题栏右上角提供切换按钮
- 按钮开启时具有激活状态
- 默认宽度 260 px
- 最小宽度 180 px
- 最大宽度 500 px
- 右侧 TOC 左边缘可拖拽调整宽度
- 显示/隐藏状态持久化
- 宽度持久化
- 尽量沿用左侧 Sidebar/TOC 的主题语义，方便已有 Custom CSS 同时生效
- 性能优先：关闭右侧 TOC 时不保留第二棵 TOC Tree

详细实现：

`docs/custom/右侧TOC实现记录.md`

### 3.2 Windows 右键菜单

状态：**已实现**

Windows NSIS 安装包新增独立的 `Open with MarkText` 右键菜单：

- Markdown 文件：`.md`、`.markdown`、`.mmd`、`.mdown`、`.mdtxt`、`.mdtext`、`.mdx`
- 文件夹本身
- 不注册文件夹内部空白处 `Directory\Background`
- 菜单带 MarkText 图标
- 右键菜单注册与安装时的“默认 Markdown 文件关联”询问相互独立
- 卸载时只删除本功能创建的 MarkText 菜单项
- 安装/卸载后通知 Explorer shell association 已变化

详细实现：

`docs/custom/Windows右键菜单实现记录.md`

## 4. 当前右侧 TOC 涉及的源码

修改：

- `packages/desktop/src/renderer/src/pages/app.vue`
- `packages/desktop/src/renderer/src/components/titleBar/index.vue`

新增：

- `packages/desktop/src/renderer/src/components/rightToc/index.vue`
- `packages/desktop/src/renderer/src/store/rightToc.ts`

原生 TOC 继续复用：

- `packages/desktop/src/renderer/src/components/sideBar/toc.vue`

没有因为右侧 TOC 而替换原有左侧 TOC 实现。

## 5. Windows 安装器自定义

Windows 安装器自定义入口：

- `packages/desktop/build/windows/installer.nsh`

当前该文件同时负责：

- 原有 Markdown 默认文件关联询问
- Markdown 文件 `Open with MarkText`
- 文件夹本身 `Open with MarkText`
- 卸载时清理对应注册表项

electron-builder 配置通过：

```text
nsis.include = windows/installer.nsh
```

把该脚本包含进 NSIS 安装包。

## 6. Windows x64 构建方式

当前优先使用已经实际验证的“两阶段构建”流程：

```powershell
cd E:\github\marktext
pnpm run build

cd .\packages\desktop
npx --yes electron-builder@26.0.3 --win nsis --x64 --publish never
```

原因是项目自带的完整 Windows 打包脚本曾在 electron-builder 的 node modules 扫描阶段长时间卡住。

因此：

- 两阶段方式是当前首选；
- `pnpm run build:win:x64` 保留为原项目官方入口，但不是本 fork 的首选本地打包方式；
- Windows 右键菜单属于 NSIS 安装器逻辑，只有完成第二阶段并安装生成的 setup EXE 后才会注册；`pnpm run dev` 本身不会创建这些右键菜单。

完整说明见：

`docs/custom/Windows_x64本地编译指南.md`

## 7. 后续功能记录规则

以后新增重要功能时，建议：

1. 单独建立 `docs/custom/<功能名>实现记录.md`；
2. 在本文件“已实现的自定义功能”中增加一项；
3. 记录涉及的完整源码路径；
4. 记录设计目标、兼容性与性能约束；
5. 如果涉及 Windows 安装或打包，更新编译指南。

这样 AI 在进入仓库后，通过根目录 `AGENTS.md` 和本文件即可迅速建立正确项目背景。
